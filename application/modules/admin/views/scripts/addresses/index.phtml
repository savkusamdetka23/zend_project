<form action="<?php echo $this->url(array('controller' => 'addresses', 'action' => 'add'))?>"> <button class="btn btn-default">Add new Address</button></form>

<h3 ><span class="glyphicon glyphicon-home text-muted"></span>Addresses</h3>

    <table class="table table-responsive table-hover">
        <thead>
        </th><th>City</th><th>Street</th></tr>
        </thead>
        <tbody>
        <?php
        if (is_array($this->addresses) && count($this->addresses)) :

        foreach($this->addresses as $addresses) : ?>
        <tr>
            <td><?php echo $this->escape($addresses['city']) ?></td>
            <td><?php echo $this->escape($addresses['street']) ?></td>


            <td>


                <a href="<?php echo $this->url(array('controller' => 'addresses', 'action' => 'edit', 'id' => $addresses['id'])); ?>">edit</a>

            </td>
		   <td>
                <a href="<?php echo $this->url(array('controller' => 'addresses', 'action' => 'delete',  'id' => $this->escape($addresses['id']))); ?>">delete</a>
            </td>
        </tr>
    <?php endforeach; ?>
    <?php endif; ?>
        </tbody>
</table>